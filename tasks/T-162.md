---
id: T-162
title: Custom waypoint — a second freely-placeable overworld marker
parent: —
owner: frontend-engineer
state: completed
branch: feat/custom-waypoint
pr: —
commit: —
review_artifact: docs/reviews/feat-custom-waypoint/final.md
created: 2026-07-20
updated: 2026-07-20
---

# T-162 — Custom waypoint

## Description
Coverage-audit #13 (`whats-new` v13): a second, freely-placeable Start-Spot-like
personal marker. Mirrors the existing start-spot exactly:
- `TrackerModel.customWaypoint: OverworldScreenCoordinate?` (independent of `startSpot`).
- `OverworldMapView` gains `customWaypoint` + `onSetWaypoint` / `onClearWaypoint`,
  rendered as an **amber diamond** (distinct from the violet start-spot ring and the
  cyan keyboard cursor), and a **Set / Clear waypoint** pair in the tile context menu.
- `OverworldSectionView` wires the closures to the model.

Purely a user bookmark — no tile-mark or routing side effects. Not persisted (save/load
is still pinned, audit #4), matching the start spot.

## Notes / audit hygiene
Fixed three stale audit lines while here — #14 (Reminder log) and #18 (Row-location)
were already shipped (`ReminderLogView`, `RowLocatorWidget`); #13 now marked done.

## Acceptance criteria
- [x] Set/clear a waypoint from the tile menu; distinct marker renders.
- [x] Independent of the start spot (each set/clear leaves the other untouched).
- [x] Build clean; 622 tests pass.

## Activity log
- 2026-07-20 — model + view + wiring, mirroring the start spot. 622 tests.
