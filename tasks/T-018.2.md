---
id: T-018.2
title: ITrackerEvents + allUIEventingLogic announcement/reminder orchestration
parent: T-018
owner: frontend-engineer
state: proposed
branch: —
pr: —
commit: —
review_artifact: —
created: 2026-07-13
updated: 2026-07-13
---

# T-018.2 — Announcement / reminder orchestration

## Description
The architecture-heavy half of T-018 (`TrackerModel.fs:1486-1750`, ~260
lines): the `ITrackerEvents` callback contract (`:1486-1508`),
`allUIEventingLogic(ite)` (`:1568-1750`) diffing previous-vs-current state to
fire announcements (heart-threshold sword-cave reminders, dungeon
completion/found/triforce-count, Triforce-and-Go, blocker "you can go back"
reminders, one-shot item nudges), the ~dozen `prior*`/`remembered*` edge-
transition trackers (`:1511-1530`), `doesPlayerHaveTriforceAndWhichDungeon-
IndexIsIt` (`:1553-1567`), and `ResetForGroundhogOrRoutersOrFourPlusFourEtc`
(`:1531-1552`).

## Acceptance criteria (refine before starting — full plan-review cycle)
- [ ] **Architecture decision:** the Swift equivalent of `ITrackerEvents` —
      reactive `@Observable` + `.onChange` observers vs. a literal delegate-
      protocol port. Evaluate explicitly; don't default to the literal port.
- [ ] Port the announcement/reminder logic for the surfaces this app has
      (cross-check `docs/domain.md` § 4.3 / `reminders.md` — much is tied to
      reminder UI not yet built; scope to what has a surface).
- [ ] Also needs recorder-warp destinations (deferred here from T-015.5) —
      `recorderDests` derivation (recorder options + HDN + vanilla dungeon
      locations).

## Out of scope
- `TriforceAndGoSummary` (done, T-018.1).

## Risks / dependencies
- Depends on T-018.1 + T-012–T-017 + reminder UI surfaces. The biggest
  architecture-judgment task in the arc.

## Activity log
- 2026-07-13 — proposed (split out of T-018).
