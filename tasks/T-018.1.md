---
id: T-018.1
title: TriforceAndGoSummary scored advisor
parent: T-018
owner: frontend-engineer
state: completed
branch: feat/triforce-and-go-summary
pr: #24
commit: —
review_artifact: docs/reviews/feat-triforce-and-go-summary/final.md
created: 2026-07-13
updated: 2026-07-13
---

# T-018.1 — TriforceAndGoSummary scored advisor

## Description
The architecture-free half of T-018: the heuristic-scored "are you basically
done, beeline to L9?" advisor. Ported from `TriforceAndGoSummary`
(`TrackerModel.fs:1439-1485`) + `unreachablePossibleDungeonSpotCount()`
(`:1422-1438`) as a pure value struct + `compute(...)`, like
`PlayerComputedStateSummary`/`MapStateSummary`. Uses
`mapState.dungeonLocations[i] != nil` as the Swift equivalent of
`GetDungeon(i).HasBeenLocated()`.

## Acceptance criteria
- [x] `TriforceAndGoSummary` (level + haveBow/haveSilvers/silversKnownInL9/
      haveLadder/haveRecorder/missingDungeonCount) ported.
- [x] `unreachablePossibleDungeonSpotCount` ported.
- [x] Scored + TAG-level (101/102/103) branching ported exactly, incl. the
      preserved upstream `haveRecorder = HaveLadder` bug (documented; makes
      102 unreachable).
- [x] 8 tests: pinned empty-grid unreachable count (41), fresh=0, full
      TAG=103, 101, silvers-in-L9, heuristic score=15, bug-mirror.

## Out of scope
- `ITrackerEvents` + `allUIEventingLogic` announcement orchestration —
  **T-018.2** (the architecture decision + reminder UI).

## Risks / dependencies
- Depends on T-012–T-016 (player/dungeon/map state). Pure computation; no UI.

## Activity log
- 2026-07-13 — split out of T-018; built `TriforceAndGoSummary.swift`, 8
  tests (175/175). Preserved the upstream haveRecorder bug for parity. Full
  review PASS. Completed.
