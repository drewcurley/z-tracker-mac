---
id: T-010
title: Overworld routing — dynamic graph layer + pathfinding search
parent: —
owner: frontend-engineer
state: completed
branch: feat/overworld-routing-dynamic
pr: —
commit: —
review_artifact: docs/reviews/feat-overworld-routing-dynamic/final.md
created: 2026-07-02
updated: 2026-07-02
---

# T-010 — Overworld routing: dynamic layer + pathfinding

## Description
`T-009` ported the static adjacency graph. This task ports the remaining
two pieces before GYR/routing-lines can reach the UI:
1. The **dynamic** layer (`OverworldRouting.fs:351-411`, `populateDynamic`):
   screen-scroll variants for mirrored vs. normal overworld orientation, and
   dynamic edges for recorder-warp destinations and marked any-roads (which
   depend on live tracker state, not just item possession).
2. The **pathfinding search** itself (`findAllBestPaths`,
   `OverworldRouting.fs:426-465`): a priority-queue breadth-first search
   that returns every optimal path (there can be ties) from a source vertex
   to a target, not just one.

## Acceptance criteria
- [x] Dynamic edges ported: screen-scroll tables (`staticMirrorScreenScrolls`,
      `staticNormalScreenScrolls`) and the recorder-warp/any-road edge
      generation (`addExtra`), grounded exactly, verified by the same
      call-count-comparison discipline `T-009` established (18 mirror + 16
      normal screen-scroll edges, matching the source line-for-line).
- [x] `findAllBestPaths` ported faithfully, including its tie-handling
      (multiple equal-cost predecessors) — the F# `Set`-backed priority
      queue's automatic deduplication is replicated with a Swift
      `Hashable`-backed `Set`, not a plain array, specifically to preserve
      that exact behavior. Port structure, don't "clean up" the algorithm
      shape — done.
- [x] Tests: known-route regression tests — a hand-verified Lost Woods
      dead-end (`(1,6)→(0,6)` costs exactly 8, since `(0,6)` has no other
      graph connection at all — traced by hand from the static edge list,
      not just "it returns something"), plus direct/two-hop adjacency,
      ladder shortcut, recorder-warp, and any-road-warp cost assertions.
- [x] Still not wired into the UI (GYR coloring / route lines) — that's the
      next follow-up, once the algorithm itself is verified correct in
      isolation.

## Out of scope
- GYR tile coloring and route-line rendering in `OverworldMapView` — a
  further follow-up once this task's algorithm is verified.
- The take-any pie-menu accelerator and hover magnifier.

## Risks / dependencies
- Depends on `T-009` — complete.
- Same transcription-risk profile as `T-009`, arguably higher: the
  pathfinding algorithm's correctness is harder to spot-check by eye than a
  static edge list. Prioritize the known-route regression tests.

## Activity log
- 2026-07-02 — proposed (seeded during T-009, not yet started).
- 2026-07-02 — completed on branch `feat/overworld-routing-dynamic`.
  Ported `convertToCanonicalVertex`, `staticMirrorScreenScrolls`/
  `staticNormalScreenScrolls`, `populateDynamic`, and `findAllBestPaths`.
  Added `Comparable` conformance to `OverworldScreenPortion`/`OverworldVertex`
  (deterministic priority-queue tie-breaking, not a reference-app concept —
  documented as such). 9 new tests, all hand-derived and cross-checked
  against the ported edge list before running (not fit-to-output): direct
  and two-hop grid adjacency costs, a Lost Woods dead-end regression
  (`(1,6)→(0,6)` forced to cost 8), the ladder river shortcut, recorder-warp
  and any-road-warp costs, and the zero-cost self-path case. 68/68 total
  tests passing. Single-operator review across all 9 hats — artifact at
  `docs/reviews/feat-overworld-routing-dynamic/final.md`.
