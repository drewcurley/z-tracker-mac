---
id: T-198
title: App-focused README rewrite + first tester DMG / GitHub Release
parent: —
owner: devops
state: done
branch: chore/readme-and-release
pr: —
commit: —
review_artifact: docs/reviews/chore-readme-and-release/final.md
created: 2026-08-10
updated: 2026-08-10
---

# T-198 — README rewrite + tester release

## Ask
Rewrite the README to be about the **app's features and how to use it** (for testers/users),
removing all mentions of the development playbook/harness. Then bundle a distributable build
and publish it so it can be handed to testers.

## What changed
- **README.md** fully rewritten: what the app is (a **spiritual successor** to Z-Tracker —
  explicitly *not* a "pixel-perfect clone"), requirements (Apple Silicon, macOS 14+), install
  (incl. the first-launch right-click→Open step for the unsigned build), getting started, a
  grouped feature tour, the `~/Documents/ztracker/` files, and credit to Dr. Lorgon111. No
  playbook / AGENTS / tasks / review-cycle references remain.
- **Release:** cut `dist/ZTrackerMac-<version>.dmg` via `scripts/make-dmg.sh` and published a
  GitHub Release with the DMG attached + tester install notes.

## Distribution note (maintainer)
The DMG is **unsigned / un-notarized** (free path — no Apple Developer ID). Testers use
right-click→Open once. `scripts/make-dmg.sh` already supports notarization: set
`ZTRACKER_SIGN_ID` + `ZTRACKER_NOTARY_PROFILE` (a $99/yr Apple Developer account) to produce a
clean double-click install. Documented for when an account exists.

## Tests
Docs/release only — no code change. Full suite still green (730).
