---
id: T-074
title: Old-man count shows X/Y (marked / expected)
parent: T-019
owner: frontend-engineer
state: completed
branch: fix/old-men-count-total
pr: —
commit: —
review_artifact: docs/reviews/fix-old-men-count-total/final.md
created: 2026-07-16
updated: 2026-07-16
---

# T-074 — Old-man count X/Y

## Description
User-reported: the info strip's OLD MEN readout was just the marked count. It
should be `X/Y` — marked over the number **expected** in that dungeon (constant
per dungeon barring specific shuffle flags), so you know when you've found them
all. Matches the reference (`DungeonUI.fs:586-595`, tooltip at `:584`).

- `DungeonOldManCounts` (TrackerCore): the vanilla `oldManCounts1Q`/`2Q` totals
  (`DungeonData.fs:69-70`) + `expected(secondQuestDungeons:dungeon:)`.
- Info strip shows `X/Y`. In HDN mode the `/Y` only appears once the slot is
  identified with a dungeon number (else the expected count is unknowable → `X`),
  and the total honors the second-quest-dungeons flag.

## Acceptance criteria
- [x] OLD MEN reads `marked/expected` using the 1Q/2Q vanilla totals.
- [x] HDN + unidentified slot → just `marked` (no `/Y`).
- [x] Unit tests for the counts; build clean; on-device shows `0/1` for L1.

## Activity log
- 2026-07-16 — added `DungeonOldManCounts` (2 tests) + `oldManText`/`expectedOldMen`
  in `DungeonMapView`; info strip shows `X/Y`. 420 tests pass; build clean debug +
  release; on-device L1 shows `0/1`. Completed.
