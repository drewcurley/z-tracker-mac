---
id: T-163
title: About footer — app version + project link
parent: —
owner: frontend-engineer
state: completed
branch: feat/about-version
pr: —
commit: —
review_artifact: docs/reviews/feat-about-version/final.md
created: 2026-07-20
updated: 2026-07-20
---

# T-163 — About / version footer

## Description
The clickable version + project-link half of coverage-audit #23 (the reference's
version/website button). Adds an **About** row to the bottom of the settings panel:
`Z-Tracker vX.Y.Z` (from the bundle `CFBundleShortVersionString`, which `build-app.sh`
fills from the top-level `VERSION` file; `vdev` when unbundled) + a `Project page ↗`
link to the repo. Ties into the versioning/self-update earmark — the version now
surfaces in the UI.

The LEGEND block half of #23 (map-symbol key below the overworld) is still open;
audit line updated to note the split.

## Acceptance criteria
- [x] Settings shows the app version and a working project link.
- [x] `appVersion` is a `v`-prefixed non-empty string (bundled or not).
- [x] Build clean; 624 tests pass.

## Activity log
- 2026-07-20 — About footer in SettingsPanelView. 624 tests.
