---
id: T-180
title: Reconcile the reference-coverage audit against the task list (kills, re-scopes, spoiler importer)
parent: —
owner: analyst
state: completed
branch: chore/coverage-reconcile
pr: —
commit: —
review_artifact: docs/reviews/chore-coverage-reconcile/final.md
created: 2026-07-28
updated: 2026-07-28
---

# T-180 — Reconcile the reference-coverage audit

## Description
`docs/reference-coverage.md` was last verified at T-165; T-166→T-179 have shipped since,
leaving it badly stale (it still listed shipped work — and all three "biggest pillars" — as
outstanding). Reconciled the doc against the full task list and applied a round of user
scoping decisions.

### Now marked done (were listed as remaining / "biggest pillars")
- **Timeline graph** — the overworld-progress line graph shipped (T-099).
- **HotKeys** — contexts (T-168) + smarts (T-169) + editor/voice-editor nav (T-170/T-170.1);
  only in-menu inline hints remain (#3b).
- **Broadcast window** — shipped as a re-scoped mirror (T-178); the reference's mouse-position
  auto-switching mode is deliberately not cloned ([[broadcast-window-rethink]]).
- **GRAB** — interaction (T-083) + user-verified on-device 2026-07-28.

### User scoping decisions (2026-07-28)
- **Killed:** LEGEND block (#23, single-use / permanent map space; recorder destination already
  handled elsewhere), mouse-hover explainer (#10) and hotkey cheat-sheet window (both violate the
  user's "if you have to explain how to use it, it isn't intuitive enough" rule — [[no-explainer-features]]),
  remaining-items hover (#16, redundant with the right-click picker).
- **Deprioritized (may not do):** overworld/mouse magnifiers (#6/#7), Show/Run Custom (#12),
  HFQ/HSQ (#15), "other randomizers" remnant (#21).
- **Kept:** Notes.txt auto-population (#20) — reads from `~/Documents/ztracker/Notes.txt`
  (`GameSave.defaultDirectory`); establishes the default-file-location convention ([[default-file-location]]).
- **In-menu hotkey hints (#3b)** — kept as the one remaining HotKeys slice (contextual, weightless).

### Re-scoped — seed/flags + spoiler importer (research-gated)
Moved "snoop seed/flags" up from "likely never" and expanded it into two related pieces, both
**research-first** (read the original `Zelda1RandoTools` source + the Z1R randomizer + z1r.fandom.com;
do NOT invent formats — [[z1r-wiki]] / [[z-tracker-conversion-goal]]):
- **Spoiler-file importer** (higher-value, macOS-friendly): import the randomizer's spoiler file and
  auto-mark everything (overworld + dungeon locations/contents). File-based, no OS snooping.
- **Seed/flags snoop**: read the emulator window title to auto-detect seed + flag string; the
  window-title read is the macOS-fragile part, the flag decode is the portable/reusable part.

## Acceptance criteria
- [x] Every §1–§4 item reflects current task-list reality (T-166→T-179 folded in).
- [x] Shipped pillars (Timeline graph, HotKeys, Broadcast, GRAB) moved to Completed.
- [x] User kills / deprioritizations / keeps applied with rationale.
- [x] Seed/flags + spoiler importer re-scoped, clearly marked research-gated.
- [x] Default-file-location convention recorded (doc §5 + memory).

## Notes
- Docs-only change; no code. The spoiler importer / snoop are future build tasks (research first).
- Follow-up logged in [[default-file-location]]: point the T-179 perf-log save panel's `directoryURL`
  at `GameSave.defaultDirectory` for consistency.
