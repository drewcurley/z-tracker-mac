---
id: T-005
title: Settings panel follow-ups — voice picker, "More settings", unconfirmed toggles
parent: —
owner: frontend-engineer
state: completed
branch: feat/settings-panel-followups
pr: —
commit: —
review_artifact: docs/reviews/feat-settings-panel-followups/final.md
created: 2026-07-02
updated: 2026-07-02
---

# T-005 — Settings panel follow-ups

## Description
`T-004` deliberately left several things disabled/deferred rather than guess
their contents or placement. Resolved all of them by reading
`Zelda1RandoTools/Z1R_Tracker/Z1R_WPF/OptionsMenu.fs` directly — the single
shared WPF component the reference app uses for *both* the startup screen's
embedded panel and the main window's "Options…" button, which is what made
the earlier "screen A vs. screen B" placement ambiguity resolvable for real.

## Acceptance criteria
- [x] "More settings…" implemented for real: a popover listing the 12
      overworld tile kinds that support hiding (`OverworldHiddenTileKind`)
      plus "Hide no-longer-relevant shop items" and "Always hide meat shops"
      — matches `TrackerModelOptions.OverworldTilesToHide` (14 fields) exactly.
- [x] "Change voice…" implemented for real: a popover listing installed
      voices via `AVSpeechSynthesisVoice.speechVoices()` with Test/Choose
      actions, gated behind the same >1-voice condition as the reference app.
- [x] Five previously-unconfirmed fields resolved by reading source directly:
      `GiveDungeonTrackerSunglasses` and `DefaultRoomPreferNonDescriptToMaybePushBlock`
      **are** on this screen (Dungeon settings column) — added.
      `HideTimer` **is** on this screen (Other column) — added.
      `RequirePTTForSpeech` is confirmed dead/commented-out code in the
      reference app itself — excluded, documented why.
      `UseBlurEffects` does not appear in `OptionsMenu.fs` at all — confirmed
      excluded from this component.
- [x] `docs/domain.md` § 4.1 and § 4.9 updated to remove "unconfirmed
      placement" language, replaced with resolved, cited facts.

## Out of scope
- Nothing carried forward — this task resolved everything it set out to.

## Risks / dependencies
- Depends on `T-004` — complete.

## Activity log
- 2026-07-02 — proposed (seeded during T-004, not yet started).
- 2026-07-02 — completed on branch `feat/settings-panel-followups`. Resolved
  by reading `OptionsMenu.fs` directly rather than guessing; discovered it's
  the same shared component behind both the startup screen and the main
  window's Options menu, which is what made full resolution possible. 3 new
  test functions (21/21 total passing). Verified visually: launched the app, opened
  the "More settings…" popover, confirmed all 14 items render correctly.
  Single-operator review across all 9 hats — artifact at
  `docs/reviews/feat-settings-panel-followups/final.md`.
